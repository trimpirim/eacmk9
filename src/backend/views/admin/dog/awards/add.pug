extends layout.pug

block heading
  h1 #{__('admin.dogs.award.add')}

block content
  .row 
    .col-xs-12.inner
      .row
        .col-xs-6
          form(action=`/admin/dog/awards/${dog._id}/add`, method='POST')
            include form.pug
        .col-xs-6