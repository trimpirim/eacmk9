extends layout.pug

block heading
  h1 #{__('admin.dynamic_content.edit')}

block content
  .row 
    .col-xs-12.inner
      .row
        .col-xs-6
          form(action=`/admin/dynamic-content/edit/${dynamicContent._id}`, method='POST')
            include form.pug
